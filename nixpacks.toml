[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "deno", "cloudflare-warp", "curl", "ffmpeg"]

[phases.install]
cmds = [
  "python -m pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[phases.build]
cmds = [
  "chmod +x start.sh"
]

[start]
cmd = "./start.sh"